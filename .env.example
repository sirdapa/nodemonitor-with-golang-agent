# ============================================================================
# .env.example — Contoh environment variables untuk docker-compose
# ----------------------------------------------------------------------------
# Copy file ini menjadi .env lalu edit nilainya:
#   cp .env.example .env
# ============================================================================

# Token autentikasi — HARUS sama antara dashboard dan semua agent.
TOKEN=dfbm1DLtdUec3epGotAK1tRaa3aYmNBa

# Port dashboard di host (yang dipakai untuk akses dari luar).
MONITOR_PORT=3000
